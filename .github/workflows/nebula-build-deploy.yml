name: Build and Deploy

on:
  push:
    branches: [main] # Set this to the branch you want to deploy from

jobs:
  build:
    defaults:
      run:
        working-directory: ./nebula

    strategy:
      fail-fast: true
      matrix:
        os: [ubuntu-latest]

    name: "Test nebula on ${{matrix.os}}"
    runs-on: ${{matrix.os}}

    steps:
      - uses: actions/checkout@v3

      - run: rustup toolchain install stable --profile minimal --no-self-update

      - name: Tailwind magic
        working-directory: nebula_server
        run: |
          npm i -g tailwindcss
          tailwindcss -i styles/tailwind.css -o ../assets/main.css

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: nebula

      - run: |
          cargo build --release

      - name: List out contents
        run: ls -l
      #
      # # - name: Deploy to Server via SCP
      # #   uses: appleboy/scp-action@master
      # #   with:
      # #     host: ${{ secrets.SERVER_IP }}
      # #     username: ${{ secrets.SERVER_USER }}
      # #     password: ${{ secrets.SERVER_PASSWORD }} # Use either password or key
      # #     #key: ${{ secrets.SERVER_SSH_KEY }}
      # #     port: 22
      # #     source: "deploy/*"
      # #     target: "/path/to/server/deployment/directory"
      #
      # # Optional: Clean up after deployment
      # - name: Clean up
      #   run: rm -rf deploy
